/**
 * The Controller Configuration
 */
import { SET_CONTROLLER_CONFIGURATION } from '../actionTypes';
import { replaceReducer } from './common';

export default replaceReducer(SET_CONTROLLER_CONFIGURATION, null);
