/**
 * Store the current grid columnd selection validator for a dashboard
 */
import { SET_DASHBOARD_CONSOLE_CREATOR_SETTINGS } from '../actionTypes';
import { replaceByIdReducer } from './common';

export default replaceByIdReducer(SET_DASHBOARD_CONSOLE_CREATOR_SETTINGS);
