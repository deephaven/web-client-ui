// import React from 'react';
// import { useSelector } from 'react-redux';
// import DashboardDataContext from '../DashboardDataContext';

// export type DashboardPanelWrapperProps = {
//   children: React.ReactNode;
//   id: string;
// };

// /**
//  * A page view with a back button in the bottom left, a title at the top, and a close button in the top right.
//  * Back and close buttons will only appear if `onBack` and/or `onClose` callbacks are set.
//  */
// export const DashboardPanelWrapper = ({
//   children,
//   id,
// }: DashboardPanelWrapperProps): JSX.Element => {
//   const dashboardData = useSelector(() => null);
//   return dashboardData;
//   // const dashboardData = useSelector(get)
//   // return <DashboardDataContext.Provider
// };

// export default DashboardPanelWrapper;
